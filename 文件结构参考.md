# HKU 课程文件结构参考

> 本文件所在目录 (`My Study Notes/`) 下, 凡以 `HKU ` 开头的课程文件夹**默认**遵循此规范.
> **例外**: 若某课 `docs/文件结构参考.md` 存在, 该课以**该文件**为准 (见 skill 优先级).

---

## 课程根目录

```text
HKU {课程代码} {课程名}/
├── readme.md                 # 文件结构说明 + 讲义索引表
├── index.md                  # AI 生成 md 跳转索引 (自动维护)
├── cheatsheet.md             # 词条/公式/定理速查
├── 课程信息.md                # 全课唯一; 多份 syllabus/intro/官网 HTML 合并于此
├── docs/                     # meta (agent / 结构 / 官网另存)
│   ├── .sync-cache.json      # 扫描缓存 (勿手改)
│   ├── 文件结构参考.md        # 可选; 仅本课覆盖父目录规范
│   └── HKU {CODE}.html       # 官网另存 (或 *.webarchive)
├── Lec/                      # 原始课件 + 同目录 AI 讲义/目录索引
│   ├── handout/              # 各讲 handout (*有文件才建)
│   └── textbook/             # optional 大部头教材 (*有文件才建; 独立编号)
├── tut/                      # 原始 tutorial + 同目录总结 (*有文件才建)
├── assignment/               # 计分 HW + 同目录题目理解/解答 (*有文件才建)
├── exercise/                 # 选做练习 + 同目录题目理解 (*有文件才建)
├── lab/                      # Lab + 同目录笔记 (*有文件才建)
├── test&quiz/                # Quiz/试卷 + 同目录解析 (*有文件才建)
├── other/                    # 其他材料 + 翻译总结等 (*有文件才建)
└── exam-review/              # 考前复习稿 (*有文件才建)
```

| 路径 | 用途 |
|---|---|
| `Lec/` | lecture slides, syllabus, chapter notes; **AI 讲义与源 PDF 同目录** |
| `Lec/handout/` | 各讲 handout; 讲次号与 slides 对齐 (*有文件才建) |
| `Lec/textbook/` | Optional 大部头教材; **独立编号**, 不占课件序号 (*有文件才建) |
| `tut/` | tutorial handout + 总结 md |
| `assignment/` | 计分 homework + 题目理解/解答 md |
| `exercise/` | 选做/补充练习 + 题目理解 md |
| `lab/` | lab sheet + 题目理解/总结 md |
| `test&quiz/` | quiz, midterm, final + 解析 md |
| `readme.md` | 目录树 + 产出索引 (文件 \| 原始材料 \| 说明) |
| `index.md` | **跳转页**: 课程内全部 AI 生成 md 的可点击链接 |
| `cheatsheet.md` | 专业名词/公式/定理/问答沉淀 |
| `课程信息.md` | 评分/大纲/政策; 「材料来源」表 |
| `docs/` | `.sync-cache.json`, 本课 `文件结构参考.md`, 官网 HTML/webarchive |

**根目录仅允许**: `readme.md`, `index.md`, `cheatsheet.md`, `课程信息.md`, `docs/`, 上述类型的材料子目录 (若有). **不要**单独创建 `notes/` 目录. **不要**把 HTML / cache / 课程结构参考留在课程根. 原始 PDF 不得长期堆在课程根目录. **Assignment / Exercise / Lab / Quiz 分目录保存, 文件名含类型前缀** (见 skill `reference-naming.md`).

**按需建目录**: 上表为类型约定. **有文件或即将写入产出时才创建对应文件夹**; **不要**预先创建空的 `assignment/` / `exercise/` / `lab/` / `tut/` / `test&quiz/` 等. 扫描发现空目录可删除. `docs/` 在首次写入 cache/HTML/结构参考时创建.

**AI 生成 md 与源同目录 (强制)**: `Lec/01-foo.pdf` → `Lec/01-foo-讲义.md` (或分章 `Lec/02-foo-Ch01-讲义.md`); `assignment/01-hw.pdf` → `assignment/01-hw-题目理解.md` 等. **禁止**再建平行的 `notes/Lec/` 树.

---

## 文件名规则

### 课程信息 (例外: 单文件, 无序号, 无文件夹)

| 类型 | 路径 |
|---|---|
| 课程信息 | `课程信息.md` (课程根) |

- 所有 syllabus / introduction / handbook **以及 `docs/` 官网 HTML** **增量合并**进此文件
- 文件内维护「材料来源」表, 列出各 PDF / HTML
- **不要**建 `course-info/` 或 `notes/` 文件夹

### 工作区根 Todo; 官网 HTML 在 `docs/`

| 路径 | 用途 |
|---|---|
| `{工作区根}/HKU courses todo.md` | **仅当前可做**; **讲义入库后按章节拆 Read**; 材料同步增补/删占位; 已完成块置顶 |
| `{课程根}/docs/HKU {CODE}.html` | 该课官网另存; 供更新 `课程信息.md` (**不**放在工作区根或课程根) |

详见 skill 内 `reference-todo.md`.

### AI 产出 (与源 PDF 同目录)

格式: **`{序号}-{原文件名 stem}-{文档类型}.md`**

| 文档类型 | 用途 |
|---|---|
| `讲义` | 课件中文详解 (**覆盖原材料全部知识点**, 可拓展) |
| `目录索引` | 大部头 lecture notes 章节目录 |
| `总结` | tutorial 总结 |
| `题目理解` | assignment / exercise / lab 中文题意 |
| `解答` | assignment / lab 中英解答 |
| `解析` | past paper / quiz 解析 |
| `翻译总结` | 其他材料中文总结 |
| `修改意见` | 用户稿件反馈存档 |
| `考前复习` | exam scope 复习稿 |

| 类型 | 路径模式 |
|---|---|
| 讲义 | `{与 PDF 同目录}/{NN}-{stem}-讲义.md` |
| 目录索引 | `{与 PDF 同目录}/{NN}-{stem}-目录索引.md` |
| Tutorial | `tut/{NN}-{stem}-总结.md` |
| Assignment | `assignment/{NN}-{stem}-题目理解.md` + `{NN+1}-{stem}-解答.md` |
| Exercise | `exercise/{NN}-{stem}-题目理解.md` (可选解答) |
| Lab | `lab/{NN}-{stem}-题目理解.md` 或 `-总结.md` |
| Quiz / 试卷 | `test&quiz/{NN}-{stem}-解析.md` |
| 其他 | `other/{NN}-{stem}-翻译总结.md` |
| 考前复习 | `exam-review/{NN}-{标识}-考前复习.md` |

- `{NN}`: 同一子目录内两位序号 `01`, `02`, …
- `{stem}`: 原始文件名去扩展名, 空格 → `-`
- **序号优先**: 材料内 Ch/Lec/Week 编号 → syllabus 周次 → 原文件名字母序
- **讲义专名**: 每份 `*-讲义.md` 内, 英文专有名词**首次**写成 `中文 (English)`, 同文件其后可只写中文
- **讲义详细度**: 须覆盖原材料全部知识点, 可拓展直觉/例子; **非定义公式须附证明/思路**; 讲义入库后 todo 按章/节拆 Read
- **Todo**: 仅当前可做; 禁止题面未到等占位; 材料与讲义更新时同步

---

## Stray 文件归位

课程根目录下出现的 PDF/DOCX/PPTX 等原始材料:

| 文件名关键词 (不区分大小写) | 移至 |
|---|---|
| syllabus, intro, handbook, lecture, lec, chapter, slide | `Lec/` (课件; 非 optional 全书教材) |
| textbook, boyd, bartle, ross, nocedal (optional 全书) | `Lec/textbook/` |
| handout (typed / 课堂) | `Lec/handout/` (若课程用 handout 夹; 否则 `Lec/` 与 slides 共享 NN) |
| tut, tutorial, worksheet | `tut/` |
| hw, homework, assignment, problem-set, problem set, ps | `assignment/` |
| exercise, extra exercise, practice (非 hw) | `exercise/` |
| lab, Lab0, take-home lab | `lab/` |
| `.zip` / `.tar.gz` (内容待判) | **先解压**再按上表归位 (见 skill §3.6); 原包可留 `lab/` 或 `docs/archives/` |
| quiz, test, exam, midterm, final, past, sample | `test&quiz/` |
| 无法判断 | `Lec/` (readme 注明) |

**不移动** (留在课程根): `readme.md`, `index.md`, `cheatsheet.md`, `课程信息.md`, 用户手写学习 md.
**归入 `docs/`**: `文件结构参考.md`, `.sync-cache.json`, `*.html`, `*.webarchive`.

---

## 文件名规范化

扫描时原始文件名按 **文件内标题为主** + **同目录 `{NN}-` 序号** 规范化 (详见 skill `reference-naming.md`):

1. 读 PDF 标题 / 章节主题 (跳过过泛课程总标题)
2. 生成 `{NN}-{title-slug}.ext` (同文件夹内按教学顺序编号)
3. **Assignment / Quiz / Exercise / Lab / Exam**: 分目录保存, slug **必须**含类型前缀 (`assignment-`/`hw`/`ps-`, `exercise-`, `lab-`, `quiz-`, `test-`/`midterm-`/`final-` 等)
4. 更新 `sync-cache`, `readme`, 同目录 md 引用与 stem

默认模式: 扩展名小写; `-` 连接; 日期 `YYYY-MM-DD`; 例 `01-syllabus-2026.pdf`, `02-chapter1-real-numbers.pdf`.

---

## 材料 → 产出

| 原始类型 | 识别 | 产出 |
|---|---|---|
| 课程介绍 | syllabus, intro, handbook, `docs/HKU {CODE}*.html` | 更新 `课程信息.md` |
| 知识点课件 | Lec slides, chapter PDF | 同目录 `{NN}-{stem}-讲义.md` |
| 大部头教材 | PDF > 40 页或 TOC ≥ 3 顶级章 | 主讲义: AskQuestion + 同目录索引/分章讲义; **optional 教科书 → `Lec/textbook/`**, 默认不生成 |
| Assignment | homework, assignment, problem set → `assignment/` | 同目录 题目理解 + 解答 |
| Exercise | extra / practice → `exercise/` | 同目录 题目理解 |
| Lab | lab → `lab/` | 同目录 题目理解或总结 |
| Quiz / 试卷 | quiz, test, exam → `test&quiz/` | 同目录 `{NN}-{stem}-解析.md` |
| Tutorial | tut handout | 同目录 `{NN}-{stem}-总结.md` |
| 其他 | reading, announcement | `other/{NN}-{stem}-翻译总结.md` |
| 用户稿件 | 用户写的草稿 | 聊天给修改意见; 可选存档 `修改意见` |
| 考前复习 | 用户给出 exam scope | `exam-review/{NN}-{标识}-考前复习.md` |

### 大部头教材

- **禁止**一次性全文生成讲义
- **Optional 教科书** (Boyd / Bartle / Ross 等) → `Lec/textbook/`, 独立编号, 默认 `optional_textbook`
- 课程主讲义大部头仍在 `Lec/`; 扫描时可自动: 更新 `课程信息.md` + 同目录 `目录索引`
- 分章讲义需用户确认范围后再生成

### 同主题多版本编号

- `01-topic-slides.pdf` 与 `01-topic-slides-long.pdf` **共享 `01`** (勿把 long 编成 `02-`)
- Handout → `Lec/handout/{NN}-topic-handout.pdf`, `{NN}` 与该讲 slides 对齐

### Duplicate

同讲次内容高度重叠的 PDF (handout vs slides vs slides-long) → cache 标记 `duplicate`, 不重复生成讲义.

---

## 扫描与同步

每次 `/course-learning` 或整理 HKU 课程时:

1. 解析结构规范 (课程内 / 父目录 / 默认)
2. 归位 stray; **规范化混乱文件名**; 官网 HTML/cache/本课结构参考归入 `docs/`; **按需建目录** (有文件才创建; 可删空占位夹)
3. 对照 `docs/.sync-cache.json` 检测新增/变更
4. 生成或更新同目录 md (**讲义须覆盖原材料全部知识点**, 可拓展); 增量更新 `cheatsheet.md`
5. 同步 `readme.md` 索引表; **重建 `index.md`**; **讲义入库后按章节拆分** todo Read
6. **同步** `HKU courses todo.md` (仅可做项; 增补 + 删占位 + 按章拆分 + 已完成置顶)

---

## 说明

- 本文件放在 **课程文件夹的父目录** (`My Study Notes/`), 作为同级 `HKU *` 课的**默认**规范
- 单门课可在 `{课程根}/docs/文件结构参考.md` **覆盖**本文件 (如 MATH3603, CCST5037)
- `Finished/HKU .../` 归档课若父目录为 `Finished/`, 需在 `Finished/文件结构参考.md` 单独维护 (或复制本文件)
